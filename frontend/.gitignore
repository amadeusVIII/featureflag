# Dependencies — never commit these, npm install recreates them
node_modules/

# Build output — generated by npm run build, not source
dist/

# Local env overrides
.env.local
.env.*.local

# Editor
.vscode/
.DS_Store